# 1 数组的遍历

```js
let done = [485, 495， 414 ]

485 ： 两个变量a ，b  a 否是1 是1 就加1  否则就重置为零
另一个变量b记录是否是最大值， 每次 是1 增加的情况，就取一次max , 最后返回这个变量

495 : 每次取间隔的最小值， 先加上最后一次的duration ，

414: 求第三大的数， 三个变量。 并且去掉重复的元素

628
```
